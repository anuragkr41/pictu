server:
  port: 8082

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/pictu_photos
    username: pictu
    password: mynewpassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true